# script_view_only_pdf_gdrive
Script Inspect Element when you open browser then u can run this code in console if u can't or did'nt work u can run it by 'allow pasting' in console and run it again brother
